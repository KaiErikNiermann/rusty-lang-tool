# Rust build artifacts
/target
**/*.rs.bk
Cargo.lock.orig

# Generated / fetched data — NOT committed (fetched on demand via `cargo xtask fetch-*`)
/resources/lt/
*.rkyv
*.dict
# nlprule engine binaries (fetched via `cargo xtask fetch-engine`)
/resources/*.bin
/resources/*.bin.gz

# wasm-pack output
/crates/rlt-wasm/pkg/

# Node
node_modules/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# insta pending snapshots
*.snap.new
